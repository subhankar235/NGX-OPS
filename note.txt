Your code changes
       ↓
GitHub checks:
✓ Dependencies install?
✓ Lint passes?
✓ Tests pass?
✓ Docker builds?



CD (Continuous Deployment)

After CI succeeds:

Code verified ✅
      ↓
Deploy to server
      ↓
Update application
      ↓
Restart containers.



ci/cd can be without docker also with it also 






.kubernetes---when we have mnay conatiner we have to use kubernetes coz only conatiner is not enough-
Cluster = Entire Kubernetes system

Worker Node = Server

Pod = Smallest deployable unit-which is goning to contain the container,

Container = Application


Deployment = Creates and manages Pods

Service = Sends traffic to Pods

Deployment = Maintains desired number of Pods

Service = Exposes Pods to users,


Control Plane = Brain of Kubernetes
ow who actually creates those 3 pods?

Not the Deployment.

The Control Plane reads the Deployment and does the work.
Deployment
    ↓
"I want 3 pods"

Control Plane
    ↓
"Okay"
    ↓
Create Pod1
Create Pod2
Create Pod3


**
Kubernetes
     ↓
Create Pods
Monitor Pods
Restart Pods
Scale Pods
Update Pods
Route Traffic